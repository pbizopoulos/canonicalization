# git+ssh://git@<domain>/<repository-path>.git				# <URL-private> remote private repository
# https://<domain>/<repository-path>.git				# <URL-public> remote public repository
# ~/<domain>/<repository-path>						# <URL-local> local repository; repository-path may contain nested namespaces
# Using a subset of https://numtide.github.io/blueprint/main/
# ~/<domain>/<repository-path>/packages/<package>			# <URL-local-package> https://numtide.github.io/blueprint/main/getting-started/folder_structure/#packages-for-packages
# ~/<domain>/<repository-path>/hosts/<host>				# <URL-local-host> https://numtide.github.io/blueprint/main/getting-started/folder_structure/#hostshostnamedefaultnixconfigurationnixdarwin-configurationnixsystem-configurationnixrpi-configurationnix
# ~/<domain>/<repository-path>/checks/<check>				# <URL-local-check> https://numtide.github.io/blueprint/main/getting-started/folder_structure/#checks-for-flake-checks
git clone <URL-*> <URL-local>						# clone repository
nix flake update <URL-*> 						# update flake lock file
nix flake check <URL-*>			 				# check whether the flake evaluates and run its tests
cd <URL-local> && nix fmt						# run formatter
cd <URL-local-package> && nix develop .#<package>			# enter <package> development environment
nix run <URL-*>#<package>						# run <package>; use the next command to run tests if the build is slow
nix run <URL-*>#<package> --extra-substituters "https://nix-community.cachix.org https://cache.nixos-cuda.org" --extra-trusted-public-keys "nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs= cache.nixos-cuda.org:74DUi4Ye579gUqzH4ziL9IyiJBlDpMRn9MBN8oNan9M="
